（c）附加题：如何修改 trie 树，使其能够高效地确定 trie 树中具有特定前缀的单词数量？描述你的解决方案的运行时间。
我们可以向trie中的每个节点添加一个numWordsBelow变量 
我们插入，我们将为插入路径上的所有节点递增此变量。 
为了确定以特定前缀开头的单词数量，我们 
可以按照前缀中的字母遍历trie。一旦我们到达终点 
对于前缀，我们返回前缀中最后一个字符的numWordsBelow，或0 
如果entrie前缀不包含在树中。如果前缀的长度为k 
那么在最坏的情况下，此代码将在Θ（k）中运行。如果我们有这样的情况 
字符串的长度将被假定为常数，那么这个运行时 
当我们去掉常数系数时，Θ（k）实际上将为Θ;（1）。