我们需要将一个包含 N 个唯一数字的数组按升序排序。请确定以下排序算法的最佳和最坏情况运行时间：
（a）当归并排序中的游程大小小于等于 N/100 时，我们对它们执行插入排序。
最佳情况：Θ( )，最坏情况：Θ( )
第一层的merge sort 的size是n
第二层是两个，每个n/2，第三层每个n/4....第七层每个n/128，这时候切换到insertion sort
runtime = mergesort + insertionsort
mergesort=7*n+128*insertion(n/128)，由于常数忽略，并且insertion最好n最坏n^2
所以最好=n+n=n，最坏=n+n^2=n^2

（e）我们运行一个我们选择的最优排序算法，已知：
最多有 N 个倒位:
最好n,最坏n
回想一下，插入排序需要Θ（N+K）时间，其中K是反转次数。如果K最多为N，则插入排序的最佳和最坏运行时间为Θ（N）
而别的最快也要nlogn
为了使我们的算法终止，我们要么需要处理每个反转，要么需要查看每个元素。
由于最多有N个反演，知道我们已经解决了每个反演问题至少需要Θ（N）时间。
查看列表中的每个元素也需要Θ（N）时间。无论哪种情况，我们都看到任何排序算法的运行时间都不能快于Θ（N）。
恰好有 1 个倒位:
最好1，最坏n
反转可能是前两个元素，在这种情况下需要恒定的时间。或者，它可能涉及末尾的元素，在这种情况下，需要N个时间。
可以很简单地证明，在最佳和最坏的情况下，没有排序算法能达到比上述更好的运行时间。