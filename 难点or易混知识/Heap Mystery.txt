给定一个表示最小堆的数组，其中每个字母代表一个唯一的数字。假设最小堆的根位于索引 0，即 A 为根。
注意，字母顺序在这里没有意义，也就是说，即使 B 在字母表中位于 C 之前，我们也无法判断 B 是否小于或大于 C。
数组：[A, B, C, D, E, F, G]
然后对最小堆执行四个未知操作。每个操作要么是 removeMin 操作，要么是 insert 操作。最小堆的最终状态如下所示。
数组：[A, E, B, D, X, F, G]
（a）确定执行的操作及其正确的顺序。第一个操作已经为您填写好了！
1. removeMin()
2.
3.
4。

首先少了C多了X回来了A，所以三个步骤为insertX,insertA,removeMin
因为C比A大(从一开始得知)，所以如果insertA发生在removeC前，那就移不出去A，所以只能在他之后，然后判断insertX发生的时候
因为插入是在右边，要让X变到左边，X只能先从右下到顶上，再从顶上下降到左下，所以只能insertX先，然后removeC，从而X替换C的位置，
然后X下降到左下，如果先removeC,那么插入X后，X最大只能替换到顶上